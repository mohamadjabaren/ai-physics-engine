PySide6>=6.6          # Qt 6 bindings (LGPL) — widgets, painting, animation
pyqtgraph>=0.13       # fast live telemetry plotting on Qt's scene graph
matplotlib>=3.7       # mathtext only: renders LaTeX without a TeX install
numpy>=1.24           # required by pyqtgraph
pytest>=7.4           # tests (development only)
